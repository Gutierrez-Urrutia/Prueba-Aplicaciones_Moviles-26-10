import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PabloGutierrez154670173',
  webDir: 'www'
};

export default config;
